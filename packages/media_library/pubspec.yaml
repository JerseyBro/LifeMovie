name: media_library
description: Normalized media access and permission abstractions.
publish_to: none
environment:
  sdk: '>=3.8.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  memory_domain:
    path: ../memory_domain
dev_dependencies:
  test: ^1.25.0
