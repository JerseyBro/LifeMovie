name: media_library
description: Normalized media access and permission abstractions.
publish_to: none
environment:
  sdk: '>=3.8.0 <4.0.0'
dependencies:
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  flutter:
    sdk: flutter
  memory_domain:
    path: ../memory_domain
  path_provider: ^2.1.6
dev_dependencies:
  test: ^1.25.0
