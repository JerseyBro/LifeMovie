# ADR-004 AI provider abstraction

Status: accepted

`AiService` is provider-neutral and defaults to `MockAiProvider`. Remote and local providers are placeholders until product discovery justifies them.
